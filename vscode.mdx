줄바꿈 (word wrap)
- 한 줄로 글을 쓸때 오른쪽으로 넘어갈 때마다 좌우 스크롤을 해줘야 하는 불편함이 있다. 이 경우 vscode 줄바꿈 설정을 해주면 됨
- vscode 에서는 이 기능을 word wrap 이라고 부른다.
- 구글 검색 : vscode 줄바꿈
- 설정(Ctrl + , 또는 Cmd + ,) → word wrap 검색 → `on` 으로 지정 
- 단축키 : alt + z, opt + z

